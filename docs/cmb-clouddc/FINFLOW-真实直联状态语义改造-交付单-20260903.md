# FINFLOW 真实直联状态语义改造 交付单（2026-09-03 17:48）

## 交付物
| 文件 | 大小 | MD5 | 构建时间 |
|---|---|---|---|
| finance-system-backend-20260903-1745.jar | 83,655,260 B | `22bf01ba249eef122a70df9ee8c4db34` | 2026-09-03 17:45 |
| web-dist-20260903-1745.tar.gz | 452,318 B | `e7fe58396f2b58bd749ebc2e37ad36ee` | 2026-09-03 17:48 |

交付目录：`tmp/finflow-release-20260903-1745/`（含 MD5SUMS.txt + README-交付说明.md）

## 本次变更内容（对应用户三点需求 17:28）
1. **状态语义真实驱动**：`BankDataQueryService` 注入 REAL 适配器清单、`ConnectionOperationsService` 注入 `CmbAdapterProperties`——`bankdata.adapter.cmb.real-enabled=true` → 返回 `REAL`/绿"已连接真实银行直联（招行 CMB）"；false → `NOT_CONFIGURED`/红"真实银行直联未连接"。移除全部硬编码 `SIMULATED` 中间态。
2. **模拟数据下线**：投影查询数据源仅 `BANKDATA`，任务仅取 realAdapterCodes 匹配项，`simulated` 字段恒 false。
3. **移除三模块**：回单查询/对账单查询/代发查询——后端 resource 白名单缩为 `balances`+`statements`（其余 404"银行侧未开通该功能"）；前端删 3 路由 + 3 菜单项 + 权限收紧 2 项。

## jar 内取证
- `BOOT-INF/classes/com/finance/system/bankdata/BankDataQueryService.class`：含 `NOT_CONFIGURED` + "已连接真实银行直联…" 字符串 ✅
- `BOOT-INF/classes/com/finance/system/operations/ConnectionOperationsService.class`：含 `NOT_CONFIGURED` ✅
- 前端 dist：`已连接真实银行直联` 在 BankDataQueryPage/pages chunk ✅；`回单查询` 文案 0 命中 ✅

## 部署步骤（部署侧执行）
```bash
cd /opt/finflow
# 1. 备份（必须先于替换）
cp app.jar app.jar.bak-20260903-1745
cp -r web-dist web-dist.bak-20260903-1745
# 2. 替换 jar + dist（tar 解到临时目录再原子换名）
md5sum -c MD5SUMS.txt   # 上传后先核对
mv web-dist web-dist.old && mkdir web-dist && tar xzf web-dist-20260903-1745.tar.gz -C web-dist && rm -rf web-dist.old
# 3. 重建镜像并重启
docker compose build app && docker compose up -d --force-recreate app
docker compose ps   # 等 healthy
```

## 验收标准
- `GET /api/bank-operations/connection/overview`：real-enabled=true 时 `enabled=true, status=REAL`（绿）
- `GET /api/bank-data/statements?accountId=4`：返回 4 笔真实流水（何桂香 0.01/0.02/0.22/0.02）
- 页面账户页/流水页：绿"已连接真实银行直联"；`.env` 置 `BANKDATA_CMB_REAL_ENABLED=false` 时全红"未连接"（两态语义）
- 导航与路由：无回单/对账单/代发入口

## 回滚
```bash
cp app.jar.bak-20260903-1745 app.jar && rm -rf web-dist && mv web-dist.bak-20260903-1745 web-dist
docker compose build app && docker compose up -d --force-recreate app
```

## ✅ 执行回执（2026-09-03 18:00-18:10，已部署上线并复验）
- 部署：jar/dist/MD5 上传 → 备份（app.jar.bak-20260903-1745 / web-dist.bak-20260903-1745）→ md5sum -c 双 OK → cp 新 jar 覆盖 app.jar → web-dist 原子换名 → `docker compose build app` → `up -d --force-recreate app nginx`（注意服务名是 **nginx** 不是 web）
- 容器取证：`/app/app.jar` MD5 = 22bf01ba…（新版）✅；nginx 挂载重解析后 /usr/share/nginx/html = 新 dist ✅
- API 复验（admin 登录，localhost 经 nginx）：
  - `GET /api/operations/connections` → `enabled:true, status:REAL, "已连接真实银行直联（招行 CMB）"` ✅（正确端点是 /operations/connections，前端 api.ts 同源）
  - `GET /api/data/statements` → REAL ✅；`GET /api/data/receipts` → 404"银行侧未开通该功能" ✅（三模块移除生效）
  - `GET /api/bank-data/statements?accountId=4` → 4 笔真实流水，sourceSystem=BANKDATA，simulated=false ✅
  - `GET /api/bank-data/balances?accountId=4` → 余额快照 816064.12（与探针实测一致），total 3 ✅
- 页面：`curl http://127.0.0.1/` → 200 + 新指纹 `index-BJDAiLL5.js` ✅
- 容器状态：finflow-app healthy / finflow-web up
- **踩坑记录（部署侧注意）**：①Dockerfile 是 `COPY app.jar`，上传带版本名的 jar 后必须先 `cp` 覆盖 app.jar 再 build，否则镜像里仍是旧 jar（本次曾漏此步）；②前端换目录用 `mv web-dist web-dist.old && mkdir web-dist` 会因 bind mount 挂 inode 导致容器看不到新目录——必须 `up -d --force-recreate nginx` 重新解析挂载；③compose 服务名为 nginx（容器名 finflow-web）
- 安全：SSH 密码 askpass 用完即删，未落任何文档
