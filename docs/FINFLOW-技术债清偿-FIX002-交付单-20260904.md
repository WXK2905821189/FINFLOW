# FINFLOW 技术债清偿交付单（FIX-002）

**日期**：2026-09-04
**触发**：用户指令「迁回 master / 解决技术债务 / 推送更新至服务器上」
**代码基线**：master @ `c42b234`（技术债修复提交，含 `f9ee80f` merge 并行批次）

## 一、交付产物

| 产物 | 路径（服务器） | MD5 | 构建时间 |
|---|---|---|---|
| 后端 jar（84 lib 完整形态，含中信 SDK） | `/opt/finflow/app.jar` | `e25267717dd51cabecd44a05226aadfd` | 2026-09-04 14:48 |
| 前端 web-dist（39 assets，主 chunk 578KB） | `/opt/finflow/web-dist/` | dist 包 `4f9330a41f39454485867ea3e5b14e69` | 2026-09-04 14:31 |

- 容器内 `/app/app.jar` MD5 已核对一致（`e2526771…`）
- 替换前备份：`/opt/finflow/app.jar.bak-20260904-techdebt-pre`（原 `11a6456c…`）
- web-dist 原地解压替换（保 inode，nginx volume 挂载不变）

## 二、变更内容（FIX-002 四项 + 一个生产缺陷）

| # | 项 | 改动 |
|---|---|---|
| D2 | `BankDataAdapterRegistry.realAdaptersEnabled` 死字段 | 删除；路由真开关唯一归 `BankAdapterCallExecutor` |
| **生产缺陷** | `ConnectionOperationsService.logs()` NPE | `.eq(condition, normalize(status))` 参数提前求值 → 不带 status 筛选即 500。已修复并补回归用例 |
| P2-1 | CI grep 断言易碎 | 豁免提升为显式 `url_allowlist` 变量 + 指路式报错 |
| P2-2 | operations 包覆盖盲区 | 新增 `ConnectionOperationsServiceTest`（Mockito 8 用例） |
| P2-3 | 前端主 chunk 798KB | Shell/Login/Forbidden 三者 React.lazy 化 → **578KB**（gzip 192KB），Edge headless 首屏实测渲染完整 |

## 三、验证记录

- 本地全量测试：**148/148 绿**（29 测试类，含 NPE 回归）
- tsc / eslint 全绿；Edge headless 首屏登录页完整渲染
- CI run `33845759212`：**5/5 全绿**（Backend / Frontend / H2 / MySQL / Release contract）
- 线上部署后复验：
  - 容器 15s healthy；Flyway 增量迁移 V16→V20 应用成功
  - `/api/operations/logs` **不带 status 筛选 → 200**（修复前必 500）
  - `/api/bank-data/statements|balances` 血缘字段在位（`bankRequestNo` + `taskStatus=SUCCEEDED`，无 `simulated`）
  - `/api/operations/connections`：REAL 状态 + 动态银行文案
  - 主 chunk `index-DwOVtMiA.js`（578,973B）与本地构建一致；`simulated`/`sourceMode` 零残留

## 四、主干迁移

- PR #2 已合并（merge commit `67ff3bd`），随后 master 又 merge 并行会话 5 提交批次（`f9ee80f`，raw-message 模块 / CITIC 字段映射 / CSV 导出 / 银行合计 V20 / 抽屉焦点修复）
- 本次技术债提交 `c42b234` 直接落在 master 并推送——**日常开发线已正式迁回 master**

## 五、遗留（不阻塞）

- kingdee 包测试覆盖（FIX-002 清偿记录中已注明后续批次）
- `docs/kingdee-openapi/` 下并行会话的未跟踪文件（含 cookies/storage-state 等疑似凭据产物）未纳入本次提交，待金蝶接入线自行处理
