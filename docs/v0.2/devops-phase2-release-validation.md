# FINFLOW v0.2 第二阶段 DevOps 发布验证

执行范围：CI、Flyway 迁移门禁、运行手册、健康检查、备份恢复/回滚要求和真实银行边界。禁止修改业务源码、接入真实银行或使用生产凭据。本报告只记录本机可复核结果和 CI 待执行项。

## 当前结论

**待 CI 与隔离 MySQL 复验，暂不生产放行。**

本阶段已把 H2 空库/重复启动、MySQL 空库/增量/重复启动、V7 厂商脚本、V8-V11 边界和幂等约束纳入 `.github/workflows/ci.yml`。新增 MySQL 作业必须在 GitHub Actions 服务容器中执行；本机没有 MySQL、客户端或 Docker，不能声称本地 MySQL 已验证。

## CI 门禁改动

### Release contract

- 检查公共迁移为 V1、V2、V3、V4、V5、V6、V8、V9、V10、V11，厂商迁移为 H2/MySQL 两份 V7。
- 检查银行流水组合唯一约束、企业范围同步任务 `request_id` 唯一索引、支付 `(company_id, idempotency_key)` 唯一约束和 V8 支付审计 `request_id`。
- 检查 H2/MySQL vendor migration 路径和数据库差异处理；检查 30 天截止时间、原始层独立事务、手动入口、定时入口和请求号边界。
- 检查银行数据模块无 HTTP 客户端/真实银行调用痕迹，仓库无证书、密钥库、私钥、SDK 二进制或明确生产凭据。

### H2 migration

使用现有后端 JAR 和 Flyway 配置：

1. 空 H2 文件库启动，等待 `/v3/api-docs` HTTP 200，确认 Flyway 应用 11 个迁移。
2. 正常停止应用，再使用同一库启动，确认 `Successfully validated 11 migrations` 且没有 `Migrating schema`。
3. 任一启动失败、迁移数量不符、健康检查失败或第二次重复迁移均阻断作业。

### MySQL migration

GitHub Actions 服务容器使用 MySQL 8.4 和仅用于 CI 的临时账号：

1. 以 Flyway target V5 启动空库，确认 V1-V5 完成。
2. 以完整目标启动同一库，确认 V6、MySQL vendor V7、V8、V9、V10、V11 依次完成，健康端点返回 200。
3. 再次启动同一库，确认只校验 11 个迁移，不新增历史记录。
4. 核对 `flyway_schema_history`、关键表、企业范围索引、幂等唯一约束、V7 遗留唯一索引处理结果和 V9 窗口/保留字段。

该作业为 CI 待执行项；本机没有等价 MySQL 环境。备份恢复也必须在隔离 MySQL 实例完成，不能由 H2 结果替代。

## 30 天原始数据清理

当前代码/迁移中的 `retention_until` 不能单独证明清理任务已运行。放行前必须取得以下证据：

- 到期、未到期和豁免数据的预览与执行结果；未到期数据、标准化数据、业务数据和审计数据不被误删。
- 清理任务号、企业范围、调度身份/操作者、策略版本、处理数量、失败原因、豁免原因、时间和 `request_id`。
- 清理/归档审计只追加、可按请求号检索且不含原始载荷或敏感值；失败重试幂等并关联原失败任务。
- `BankDataRetentionOverdue` 告警的触发和处置记录；D1 清理/归档策略未决时不得声称长期合规完成。

未取得上述运行证据前，状态为“待执行”，不是通过。

## 任务、请求号与健康检查

手动和定时任务必须共享任务状态、幂等键和审计模型。证据至少覆盖：企业、连接、适配器 `MOCK`、任务号、`X-Request-Id`、银行请求号（如有）、开始/完成时间、状态、数量统计、错误类别和审计引用。

健康检查使用 `/v3/api-docs` HTTP 200 作为当前应用存活检查；Flyway、数据库连接、生产必填变量或关键 Bean 初始化失败时不得进入健康池。定时无心跳、任务超时、失败/部分成功无解释、请求号断链分别对应 `BankDataScheduledSyncMissing`、`BankDataSyncStale`、`BankDataManualSyncFailed` 和追踪缺失告警。

## 备份恢复与回滚

MySQL 测试服务器必须先证明备份可恢复到独立实例，再执行 V1-V5 基线、V6/V7/V8/V9/V10/V11 增量和重复启动。发布回滚先停止调度和流量放量，保留任务/请求/审计证据，切回上一已验证应用制品，确认健康检查和关键只读 API，再决定保留兼容数据库版本或按备份恢复/前向修复迁移处理。禁止手工修改 `flyway_schema_history` 或对已发布迁移执行逆向删除。

## 当前验证与未执行项

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| CI 静态迁移/幂等/边界门禁 | 已补充，待远端运行 | 本地已完成文本与差异检查；新增作业需在 GitHub Actions 执行。 |
| Java 17/Maven 后端 | 已验证 | 使用现有 Maven 执行测试，结果 18/18 通过。 |
| H2 空库/重复启动 | CI 待执行 | 本地此前的 H2 文件关闭行为未形成可靠重复启动证据；以新增 CI 作业为准。 |
| MySQL 空库/V1-V5 增量/V7/V8/V9/重复启动 | 本机已验证，CI/隔离环境待执行 | 2026-08-28 使用本机 `127.0.0.1:3306/Finflow` 完成既有库的迁移恢复与启动验证：Flyway V1-V9 全部成功，应用连接本机 MySQL 后登录、当前用户、同步任务、流水和余额只读 API 返回 200。该结果不替代 CI 空库、隔离 MySQL、阿里云 RDS 或备份恢复验证。 |
| 备份恢复/回滚 | 未执行 | 需要隔离 MySQL 实例、备份物料和发布制品。 |
| 30 天清理/审计/失败告警 | 待执行 | 文档已固化证据要求，不能用 `retention_until` 字段替代运行证据。 |
| 真实银行 SDK/API、证书、生产密钥 | 保持 0 | 未接入、未配置、未调用；任何非零调用或材料入库均为 P0。 |

## 发布判定

当前不发布 v0.2。必须先取得 CI H2/MySQL 作业成功、隔离 MySQL 备份恢复记录、30 天清理审计/告警证据和回滚演练记录；真实银行仍需独立 SANDBOX 准入，不得把 MOCK 或 CI 迁移通过描述为真实银行已接入。

## 本机 MySQL 连接验证（2026-08-28）

- 连接目标为用户提供的本机 `127.0.0.1:3306/Finflow`，凭据仅通过当前进程环境注入，未写入代码、配置、脚本、日志或文档。
- 首次启动发现 V6 在 MySQL 不支持的 `DROP INDEX IF EXISTS` 语句处失败。数据库当时没有业务流水数据；仅按已完成的部分 DDL 补齐缺失企业字段、索引、支付/审计/会话表，并以当前迁移校验值修复 V6 失败记录，未清空、重建或覆盖业务表。
- 应用随后正常完成 V7、V8、V9，Flyway 历史显示 V1-V9 成功；本机服务启动后，`/api/auth/login`、`/api/auth/me`、`/api/bank-sync-jobs`、`/api/bank-data/statements`、`/api/bank-data/balances` 返回 HTTP 200。
- `/v3/api-docs` 本轮仍返回 HTTP 500，属于现有 OpenAPI 文档生成缺陷，不能作为健康检查通过证据；业务只读接口和数据库迁移验证不受影响，需单独修复。
- 本次仅验证本机已有库的增量恢复与应用连接，不等同于 MySQL 空库迁移、重复启动、备份恢复、阿里云 RDS 或生产放行；真实银行 SDK/API、证书、生产密钥和外部调用保持 0。
