# FINFLOW v0.2 常见问题与避坑记录

本文记录开发、联调、测试和部署中已经发现或高概率发生的问题。每次新增问题时，补充现象、根因、解决方式和回归用例。本文不得记录密码、Token、私钥、证书内容、完整账号或生产地址。

## 1. 当前已发现问题

| 编号 | 问题 | 现象 | 根因/风险 | 规避与验证 |
| --- | --- | --- | --- | --- |
| PIT-001 | 自动入账缺企业隔离 | 用户可通过批次/流水 ID 查询或操作其他企业数据 | 旧版 `statement_*` 表缺少 `company_id`，服务层使用全局查询 | 所有表补企业归属，服务端从会话解析企业；用两个企业的同 ID 资源测试列表、详情、复核、制证、对账和审计 |
| PIT-002 | V7 厂商迁移发布基线 | V7 按 H2/MySQL 分目录执行，公共 V8-V11 继续按顺序执行；MySQL 尚未实跑 | 不同数据库的遗留全局流水号唯一约束处理可能不一致 | 在隔离 MySQL 覆盖 V1-V5 升级、厂商 V7、V8-V11、重复启动和索引结果；禁止手工修改 Flyway 历史 |
| PIT-003 | 原始层与下游同事务 | 标准化或业务写入失败后，原始响应和同步日志也消失 | 原始保存、标准化、业务写入被同一个事务包住 | 原始响应和同步日志先独立可靠落库；注入下游失败，确认原始引用仍可追溯且无半成品业务记录 |
| PIT-004 | 只测单元测试就宣布通过 | 单元测试通过，但跨企业、迁移和端到端仍有缺陷 | 单元测试不能覆盖数据库约束、认证上下文和页面链路 | 发布前必须执行集成、迁移、API 权限、双企业和 E2E 测试；报告明确区分已执行与未执行 |
| PIT-005 | Maven 使用错误本地仓库 | Maven 尝试写入 `C:\.m2` 或因权限失败 | 受限环境下未显式指定可写仓库 | 使用项目内受控本地依赖目录；命令行只记录路径，不记录凭据；构建日志确认 Java 17/Maven 版本 |
| PIT-006 | 前端构建工具不在 PATH | `pnpm build` 报 `node is not recognized` | pnpm 脚本找不到工作区 Node，而不是代码必然有错 | 先确认 Node/pnpm 路径，再执行 TypeScript 和 Vite 构建；生产构建通过不等于 E2E 通过 |
| PIT-007 | 前端包体过大 | 构建成功但出现超过 500 kB 的 chunk 警告 | 当前应用将较多页面打入单一入口 | 记录为 P2 性能问题；后续按路由拆包、手动分 chunk，并用首屏性能基线复验 |
| PIT-008 | 把前端隐藏当成安全控制 | 菜单隐藏但直接请求接口仍可访问 | 仅做了 UI 权限判断，缺少服务端方法级鉴权 | 每个读写接口都做服务端鉴权；使用无权 Token、深链和伪造资源 ID 验证 401/403 与无副作用 |
| PIT-009 | 生产配置带开发默认值 | 生产可能连接 localhost 或使用默认数据库用户 | `application-prod.yml` 使用了不安全的默认值 | 生产 DB URL、用户名、密码、JWT 密钥均必须由受控环境注入；缺失时启动失败且不回显值 |
| PIT-010 | 日志泄露敏感数据 | SQL、原始报文、账号、Token 或错误堆栈出现在日志/审计 | 调试日志、原始响应或异常直接输出 | 使用虚拟秘密探针扫描应用日志、审计、错误响应、前端 DOM/缓存、导出和告警 |
| PIT-011 | `UNKNOWN` 被当作可重试或成功 | 外部超时后重复执行，可能产生第二次外部提交 | 未将未知结果与查询/对账/人工处置闭环绑定 | `UNKNOWN` 必须冻结外部提交；仅通过受控查询、对账、补单和审计处置后改变状态，并覆盖并发 API/E2E 测试 |
| PIT-012 | `request_id` 只覆盖任务、不覆盖资金审计 | 可从银行同步追溯请求，但无法从支付审计完整关联创建、审批、执行和未知结果 | 各域采用不一致的请求关联模型 | 为支付及其审计确定稳定的请求编号存储/查询契约；复验权限拒绝、幂等冲突、`UNKNOWN` 和人工处置链路 |

## 2. 中信 SDK 联调注意事项

| 编号 | 注意事项 | 正确做法 |
| --- | --- | --- |
| CITIC-001 | SDK 初始化只能执行一次 | 将初始化放在应用生命周期中单例执行；重复初始化可能触发 `ETSK004` |
| CITIC-002 | 证书下载码一次性 | 证书目录需提前创建并具备读写权限；下载成功后保存证书和稳定 token，不要反复尝试下载 |
| CITIC-003 | Token 与证书绑定 | 自定义 token 一旦变更，旧证书可能不可用；容器环境必须使用稳定、受控存储的 token |
| CITIC-004 | IP/MAC 是银行侧白名单 | 本机内网 IP 不是银行要的出口 IP；切换网络、NAT 或网卡后要重新确认白名单 |
| CITIC-005 | 外层和内层报文不同 | 外层通常为 `DLGECOMM`；业务交易码在内层；内层 XML 使用 GBK，再进行 Base64 封装和解码 |
| CITIC-006 | 外层成功不等于业务成功 | 外层 `AAAAAAA` 只说明 SDK/通信层成功；必须继续解码并判断内层业务响应 |
| CITIC-007 | 示例机器信息采集不可照搬 | 示例可能执行系统命令读取硬件信息；生产容器应采用受控稳定 token/MAC 方案，不执行未审查命令 |
| CITIC-008 | 银行查询有窗口和分页限制 | 流水查询按银行允许的日期窗口拆分，处理分页、空页、重复页和乱序；不能把部分结果显示为完整成功 |
| CITIC-009 | 外部编号长度要单独设计 | 银行不同接口对 `clientID` 长度可能不同；内部 `requestId` 与不超过银行限制的外部幂等号分开保存 |
| CITIC-010 | 银行状态需要业务映射 | `AAAAAAE`、`EEEEEEE`、`UNKNOWN` 等状态不能直接映射为付款成功；需按银行文档和沙箱确认后进入状态机 |

## 3. 数据链路不可违反的规则

1. 银行请求和响应先进入受控原始表，并写入同步日志。
2. 原始记录保存后，才允许执行统一数据模型转换。
3. 标准化数据通过校验、去重和企业范围检查后，才能写入业务表。
4. 前端只调用 FINFLOW API，只查询业务表或查询投影。
5. 原始报文、银行专有字段、证书、私钥、Token 和完整账号不得进入普通 API、前端、导出或客户端日志。
6. 定时同步、手动同步和重试必须共用任务、请求编号、幂等和审计模型。
7. 重试不能创建第二条流水、第二个凭证、第二笔支付或第二次余额变化。

## 4. 测试与发布检查

- [ ] 新增表或接口明确 `company_id` 来源，不能接受客户端企业 ID 作为授权依据。
- [ ] 迁移文件按版本顺序存在，未发布迁移不被重写，空库/增量库均已验证。
- [ ] 原始层失败、标准化失败、业务写入失败均有故障注入用例。
- [ ] 后端单测通过后，仍完成集成、权限、双企业、迁移和 E2E 测试。
- [ ] 前端 TypeScript、生产构建、页面状态和 API-only 检查均有记录。
- [ ] 生产配置无默认数据库、开发 JWT 密钥、调试日志和测试凭据。
- [ ] 真实银行 SDK/API 未经单独沙箱准入时，系统保持模拟/未启用状态。
- [ ] 每个 P0/P1 缺陷都有责任角色、修复版本、复验用例和证据。

## 5. 待决策事项

- 原始报文和同步日志 30 天后的清理或归档方式。
- 定时任务责任人、告警接收人、升级路径和 SLA。
- 银行超时但可能已受理时的补单、查询和人工确认流程。
- 测试/非生产环境允许使用真实业务数据的具体范围、脱敏方式、导出限制和退出日期。

## 6. 本阶段核对状态（2026-08-27）

- `PIT-001`、`PIT-003`：H2/API 集成复验已通过；MySQL 兼容性和浏览器 E2E 未执行，不能扩展为生产放行。
- `PIT-002`：保持 P1，直至 V7 被纳入 Git/制品基线并通过隔离 MySQL 升级验证。
- `PIT-004`：本阶段已有 11 项测试通过，但未执行项仍须在报告中保留，不能以测试总数替代发布验收。
- `PIT-010`：运行时日志、审计、浏览器 DOM/缓存、导出和告警探针扫描仍未执行。
- `PIT-011`、`PIT-012`：V8 和 H2/API 集成已覆盖最小 UNKNOWN 查询/人工确认与支付审计 `request_id`；真实银行查询/补单、并发处置和生产审计探针仍待后续专项验证。

## 7. 第二阶段银行数据接入常见问题

以下条目是 PH2 的风险与待执行验证口径，不代表问题已修复或用例已通过。相关验收状态仅允许在测试报告中记录为“待执行”或“待银行物料”，不能用设计文档、MOCK 页面或静态检查代替运行时证据。

| 编号 | 问题 | 现象 | 根因/风险 | 规避与验证 |
| --- | --- | --- | --- | --- |
| PIT-013 | 窗口闭区间与晚到数据未定义 | 相邻查询重复边界流水，或空页/乱序后提前结束，晚到流水永远漏掉 | 起止时间包含规则、时区、游标推进和补拉窗口没有形成契约 | 用 `PH2-BD-05` 覆盖闭/开区间、首尾边界、空页后续页、重叠、乱序和晚到；保存窗口摘要、游标和去重依据；银行真实窗口语义待银行物料 |
| PIT-014 | 只写 `retention_until` 没有清理闭环 | 数据标记为到期但从未清理/归档，或清理误删业务记录、审计引用 | 只有字段设计，没有可执行任务、失败重试、豁免和清理审计 | `PH2-BD-04` 必须推进时间并实际执行预览、清理/归档、失败重试和豁免；审计记录范围、数量、操作者/任务、时间、结果和 `request_id`；在规则未决时不得宣称合规完成 |
| PIT-015 | `UNKNOWN` 被错误推进 | 未知或超时结果被当成功、可重试或新建支付，造成重复外部副作用 | 没有区分明确失败与未知结果，缺少同幂等号查询/对账/人工处置闭环 | `PH2-BD-07` 覆盖四类状态、重复/乱序/超时；未知只能保持中间状态或待对账，不自动成功或新发；确切银行语义待物料，MOCK 不能证明真实映射 |
| PIT-016 | 把 MOCK 结果称为真实银行结果 | 页面显示“同步成功”即被理解为银行直联已验证 | 模拟器没有真实协议、签名、字段、限流或回执语义，却缺少来源标识和发布边界 | 所有 MOCK 结果显式标识 `MOCK`；`PH2-BD-12` 核验真实银行调用为 0、真实模式未启用/快速失败；SDK/API、证书、沙箱和文档未到位时保持“待银行物料” |
| PIT-017 | 原始层与下游同事务回滚 | 标准化或业务写入失败后，原始响应和同步日志也消失，无法排障或审计 | 原始保存与标准化/业务写入共享事务，或下游异常触发全链路回滚 | `PH2-BD-01` 注入原始、解析、标准化、业务和审计故障；原始层必须独立可靠落库，下游失败保留原始引用；原始写入失败则禁止进入业务层 |
| PIT-018 | 前端暴露原始数据或银行专有字段 | 原始报文、银行字段、响应正文出现在 API、DOM、缓存、控制台、导出或错误页 | 前端绕过 FINFLOW API，或后端 DTO 直接复用 Adapter 响应 | `PH2-BD-02`、`PH2-BD-10`、`PH2-BD-11` 检查单向链路、深链 RBAC 和全渠道扫描；前端只显示服务端业务投影，不以隐藏菜单作为控制 |
| PIT-019 | 跨企业 `request_id` 或编号泄露 | 使用另一企业的请求号、任务号或银行侧编号可枚举或查询数据，错误响应暴露存在性 | 请求上下文未绑定企业，编号顺序可预测，日志/审计查询缺少企业过滤 | `PH2-BD-08`、`PH2-BD-09` 使用双企业相同业务数据和伪造编号；服务端强制企业上下文，拒绝无副作用且不泄露；保存 API、数据库和审计证据 |
| PIT-020 | 银行物料边界被绕过 | 真实模式因配置开关、默认端点或凭据注入误启用，或用 MOCK 证据放行真实接入 | 缺少真实调用 0 的运行时核查、出站闸门和发布门禁 | `PH2-BD-12` 扫描制品/配置、核对网络出口和 Adapter 计数；真实凭据、私钥、生产证书和生产端点始终禁止；必须有银行 SDK/API、沙箱和授权准入记录后才可开展真实协议验证 |

## 8. 第二阶段发布前核对

- [ ] `PH2-BD-01..06`、`PH2-BD-08..12` 均有运行时/API/E2E、数据库或安全扫描证据；未执行项明确为“待执行”，不得写成已通过。
- [ ] `PH2-BD-07` 的状态码定义、回执、查询/补单和重试语义已有银行文档与沙箱证据；物料未到位时明确为“待银行物料”，不宣称真实银行接入。
- [ ] 原始层先落库、企业隔离、RBAC、组合去重、`UNKNOWN` 防误推进、清理审计和全渠道敏感扫描均无未评估 P0；静态检查不替代 API/E2E。
- [ ] 手动/定时任务共用幂等和状态机；请求编号跨 Adapter、原始层、业务表、前端错误、审计和清理任务可追溯，且没有跨企业泄露。
- [ ] 真实银行调用计数为 0，MOCK 数据源显式标识，真实模式未启用或快速失败；真实凭据、私钥、Token、生产端点和证书不在任何渠道。
- [ ] RA-DATA 风险接受有批准范围、到期日、脱敏、访问/导出限制和清理证据；不得放宽安全硬门禁。

## 9. 本轮第二阶段复验口径（2026-08-28）

- 后端 `mvn -Dmaven.repo.local=.m2-local test` 实际结果为 18/18 通过，且 H2 中 Flyway v1-v9 成功执行；这只能作为当前单测/API 集成证据，不能把 PH2-BD-01..12 矩阵改写为已通过。
- 已取得证据覆盖 MOCK Adapter 稳定响应和终止空页、双企业 API 隔离、重复同步请求复用任务、原始响应在标准化失败后保留、窗口分页/组合去重、30 天清理标记与业务记录保留、定时任务请求身份复用。
- 前端 TypeScript 检查与生产构建本轮在受控构建环境通过，但有单一入口包体超过 500 kB 的性能告警；本机 H2/MOCK 浏览器已覆盖管理员登录、六类查询点击、未启用边界、`1440/1024/375` 无页面横向溢出、375px 抽屉导航与 DOM/控制台初扫。无权角色深链、缓存/网络、导出和运行时全渠道敏感扫描仍未执行，构建通过不得替代这些证据。
- MySQL 迁移/兼容性、生产候选环境出站与配置核验、全渠道脱敏扫描、真实银行 SDK/API/沙箱均未执行；真实银行调用保持 0，不能用 MOCK 结果宣称直联可用。
- `UNKNOWN`、`PENDING` 和四类银行状态码的最终语义仍以银行物料和沙箱为准；实现中的中间态安全检查不等于真实回执、查询/补单或异步乱序验收通过。
- 发布结论保持为“已有 H2/API 与单测证据，PH2 银行底座验收未完成，真实银行调用为 0”；任何发布签字仍须逐项补齐矩阵证据、责任人、截止日期和残余风险处理。

## 10. 第二阶段质量收口风险登记（2026-08-27）

以下是当前候选版本的收口口径；“已通过”均带有 H2/API/单测范围，不代表生产验收完成。

| 风险级别 | 编号 | 常见误判 | 当前证据与处置 |
| --- | --- | --- | --- |
| P0 | PIT-021 | H2 的 V11 迁移成功等于 MySQL 已验证 | H2 Flyway v1-v11 与后端测试可复核；MySQL 空库、增量、V7-V11、重复启动和备份恢复未执行。按 DevOps 门禁保持阻断，须由隔离 MySQL/CI 证据解除。 |
| P0 | PIT-022 | MOCK 日志中的“无外部网络调用”可替代生产调用为 0 | 当前仅有 MOCK/H2 证据；生产候选环境出站、DNS/白名单、制品和运行时计数未核验。真实 SDK/API、凭据、证书或生产端点出现即停止发布并按安全事件处理。 |
| P0 | PIT-023 | 原始层有 `retention_until` 或 H2 清理测试就等于先落库和清理合规 | H2 已证明标准化失败后原始消息保留、过期 payload 标记和业务记录保留；隔离 MySQL 事务、调度失败重试、豁免、审计不可篡改和告警仍未执行。 |
| P0 | PIT-024 | 菜单隐藏或服务端有六个资源路由就等于六类查询安全 | `balances`、`statements`、`receipts`、`reconciliations`、`payments`、`payroll` 的完整角色/API/E2E、双企业、空态/失败态和脱敏证据尚未齐备。越权、存在性泄露或跨企业副作用必须阻断。 |
| P1 | PIT-025 | 单实例重复请求通过就等于手动/定时并发幂等 | 当前集成测试验证同请求复用任务和定时请求身份；多实例锁竞争、MySQL 唯一索引、网络重试和告警未执行。补齐前不能扩大为生产幂等结论。 |
| P1 | PIT-026 | DTO 脱敏实现或静态扫描等于敏感数据全渠道不泄露 | 浏览器 DOM/缓存/控制台、日志、审计、导出、指标、告警、对象存储和构建制品扫描未执行。保留为 P1；一旦发现秘密或完整原始报文，升级为 P0。 |
| P1 | PIT-027 | `UNKNOWN`/`PENDING` 的本地映射等于银行状态语义已确认 | 当前仅能证明安全中间态和 MOCK 行为；四类状态码定义、异步回执、查询/补单、重试和乱序仍待银行文档与沙箱。不得据此放行真实银行链路。 |
| P1 | PIT-028 | 静态旧查询路由抢占统一投影路由 | `/api/bank-data/balances` 或 `/statements` 被明细控制器优先匹配，导致六类页面返回结构不一致 | 公开页面统一使用 `/api/bank-data/{resource}` 投影契约；旧明细路径迁移为 `balance-snapshots` 与 `statement-records`，并用 API 集成测试覆盖六类路由、RBAC 与企业范围请求号。 |
| P1 | PIT-029 | 仅以窄图标侧栏代替移动端导航抽屉，或将 32px 按钮当作合格触控目标 | 375px 虽可能没有横向滚动，但三级导航不可发现、关键操作难以触达，违背移动验收约束。 | 浏览器以 375px 检查导航抽屉、三级数据查询路径、选中后关闭、页面横向溢出和主要操作 `>=44px`；桌面侧栏逻辑保持不变。 |
| P1 | PIT-030 | MySQL 迁移失败后直接重启，或把失败记录当作已回滚 | MySQL DDL 可能已部分提交，Flyway 失败记录会阻止后续版本；盲目重跑会遇到重复列、表或索引，手工清空数据库又可能破坏业务数据。 | 先只读盘点 `flyway_schema_history`、表、列、索引、约束和数据量；依据失败语句做最小、可恢复的补偿，核对迁移当前校验值后再继续 V7-V9；记录恢复动作，禁止无授权重建或删除业务库。 |

### 10.1 收口清单

- [ ] 六类查询均完成 API/E2E：企业隔离、RBAC、无副作用拒绝、空态/失败态和响应脱敏有证据。
- [ ] MySQL/CI 完成 V1-V11（含厂商 V7）空库、增量、重复启动、约束和备份恢复；H2 结果不替代 MySQL。
- [ ] 原始层先落库、30 天清理、失败重试、豁免、审计和告警在运行环境完成闭环证据。
- [ ] 生产候选环境确认真实银行调用为 `0`，MOCK 标识、出站限制和凭据/证书扫描结果齐全。
- [ ] 浏览器及日志、审计、导出、指标、告警、对象存储、制品全渠道敏感扫描完成；未执行不得标通过。
- [ ] 在上述门禁完成前，发布结论保持“不放行 v0.2”，真实银行 SDK/API 与沙箱另行授权验收。
