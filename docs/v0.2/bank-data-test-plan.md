# FINFLOW v0.2 银行数据接入测试计划

## 1. 目标与范围

本计划验证 v0.2 银行数据接入底座从银行适配器模拟器到 FINFLOW 业务表和前端查询的受控链路：企业隔离、定时与手动同步、原始报文、标准化、组合去重、回执、重试、审计和领域一致性。

受测处理顺序必须为：

```text
适配器模拟器或银行 SDK/API
-> 同步任务与不可变请求摘要
-> 原始报文与同步日志
-> 标准化映射
-> 校验、组合去重与领域服务
-> 业务表、审计和查询投影
-> FINFLOW API
-> 前端
```

前端不得访问适配器、SDK 响应、原始报文、同步日志或银行专有字段；原始层、标准化层和业务层均为不可相互覆盖的历史事实。

**本计划不授权真实资金操作。** 真实凭据、私钥、访问令牌、生产证书和生产端点在所有环境、测试数据、日志、审计、导出、浏览器存储及截图中均禁止使用或出现。

## 2. 执行标记与风险接受

| 标记 | 执行环境与含义 | 发布判断 |
| --- | --- | --- |
| **SIM** | 适配器模拟器、虚拟企业和合成数据即可验证。模拟器必须可注入确定成功、业务失败、超时、重复回执和乱序回执。 | P0 用例必须通过。 |
| **INT** | 前后端、数据库、任务调度、日志/审计和模拟器已部署的非生产联调环境。 | P0 用例必须通过，留存请求编号和数据库证据。 |
| **RA-DATA** | 受“测试/非生产环境临时允许真实银行业务数据”风险接受影响。仅可使用已批准范围内的真实业务数据副本或银行沙箱业务数据；仍遵守最小访问、脱敏、加密、审计、禁止不受控导出和退出条件。 | 只能作为临时风险接受下的联调证据，不能视为生产放行批准。正式沙箱联调前必须复核数据范围、角色、脱敏、导出、保留和清理。 |
| **SANDBOX** | 仅在银行 SDK/API、测试证书、签名规则、沙箱端点、错误码说明和授权故障矩阵就绪后执行。 | 未完成不得将模拟器通过表述为真实银行接入通过。 |
| **PROD** | 候选生产环境的只读配置、网络、监控和数据保护核查；不得发送真实资金指令。 | 生产放行前必须通过。 |

### 风险接受适用边界

- 标有 **RA-DATA** 的用例因需要核对银行字段映射、分页窗口、历史业务数据边界或回执语义，可能临时使用获批的真实业务数据。该风险接受**不**允许真实凭据、私钥、Token、生产证书或生产端点。
- 即使适用 **RA-DATA**，原始报文、日志、审计、导出、错误响应、浏览器控制台和前端本地存储仍须脱敏；仅受授权角色可查看受控最小化内容，且必须有访问和清理审计。
- 未标 **RA-DATA** 的用例必须优先使用合成数据或银行沙箱数据。生产环境不应使用该风险接受作为降低隔离、脱敏、审计或外部调用控制的理由。

## 3. 准入条件、角色与测试数据

### 准入条件

- 候选版本已部署，迁移完成，且可查询任务、同步日志、原始报文元数据、标准化记录、业务记录、审计与查询投影。
- 模拟器支持：分页、固定银行侧请求号、成功、明确失败、超时/未知、重复回执、乱序回执和重复拉取注入。
- 定时调度器可在隔离窗口触发；手动触发接口要求受控权限与幂等键；所有路径产生 `request_id`。
- 原始报文采用加密存储或受控密文，保存 `received_at`、`retention_until`、内容摘要、方向、企业、任务及银行侧请求号；普通 API 和前端无直接读取端点。
- 第二企业准备相同银行账户标识、相同流水号、相同时间金额组合和相同银行侧请求号，用于企业隔离验证。

### 最小测试角色

| 角色 | 允许能力 | 不允许能力 |
| --- | --- | --- |
| 同步操作员 | 授权手动创建本企业同步任务、查看本企业任务结果 | 查看原始报文明文、跨企业任务、绕过幂等和审计。 |
| 调度服务账号 | 仅以受控服务身份运行定时任务 | 以任意企业身份读取/写入或跳过企业范围。 |
| 业务查看者 | 查看经过授权、脱敏的业务投影与查询结果 | 查看原始报文、同步日志敏感字段、触发任务或修改业务状态。 |
| 受控审计员 | 在环境策略许可时查看最小化原始报文/日志证据 | 导出明文、修改/删除原始数据、任务或审计。 |
| 无权用户 | 无银行数据权限 | 列表、详情、原始层、任务、回执与查询投影访问。 |

### 固定数据集

| 数据集 | 内容 | 用途 |
| --- | --- | --- |
| `BD-BASE-01` | 两笔收入、两笔支出、唯一银行流水号、相同企业的启用账户、精确到两位小数金额 | 完整拉取、标准化、自动入账投影和查询事实。 |
| `BD-DUP-01` | 重复银行流水号；无稳定流水号但相同企业/账户/交易时间/金额的记录；第二企业同值记录 | 组合去重、降级判定和企业隔离。 |
| `BD-RECEIPT-01` | 对同一外部幂等号的 `ACCEPTED`、最终成功、明确失败、重复成功、乱序成功/失败回执 | 异步回执、支付尝试与转账状态机。 |
| `BD-PAGING-01` | 多页数据、页间重复、页间乱序、窗口重叠和空页 | 适配器分页、手动/定时重叠与去重。 |
| `BD-FAIL-01` | 解析失败、映射版本不识别、数据库唯一冲突、原始报文落库失败、超时、调度重启 | 原始先落库、恢复、失败重试和一致性。 |
| `BD-RA-01` | 已批准的非生产真实业务数据最小副本或银行沙箱业务数据，不含任何凭据 | **RA-DATA** 字段映射、历史窗口、分页与脱敏复核。 |

## 4. 核心不变量

| 对象 | 必须满足的约束 |
| --- | --- |
| 企业范围 | `company_id` 由服务端认证上下文决定；连接、任务、日志、原始报文、标准化记录、业务表、回执与查询投影均强制过滤。客户端企业 ID、Header、URL 或请求体不能扩大范围。 |
| 同步任务 | 定时和手动均先创建任务、请求编号和不可变请求摘要；同一业务窗口/操作的重试使用同一幂等语义，不能绕过任务、审计或原始落库。 |
| 原始层 | 银行请求/响应及同步日志在标准化、业务写入前持久化；原始消息不可由业务写入覆盖，普通前端/API不可读取；接收后保留 30 天，并有可配置清理/归档、豁免与清理审计。 |
| 标准化层 | 每条记录含来源系统、来源记录 ID、映射版本、原始消息引用、来源时间和企业；银行专有字段不扩散到通用业务表或前端。 |
| 去重 | 对流水号和“企业 + 银行账户 + 交易时间 + 金额”执行服务端判重及数据库企业范围唯一约束；命中返回既有记录和依据。无稳定流水号时记录降级判定与人工复核原因。 |
| 回执与支付 | `ACCEPTED`、超时和未知结果仅可进入等待异步回执、待对账或人工处置状态；只有明确最终成功回执才可进入 `EXECUTED`。重复/乱序回执不得重写最终事实、重复交易或重复余额变更。 |
| 前端 | 只调用 FINFLOW API 获取经权限过滤的业务表/投影；无数据、未启用、加载和失败状态必须区分，失败显示请求编号，不能缓存/渲染原始银行响应。 |

## 5. 可执行测试用例

| ID | 优先级 | 环境 | 场景与操作 | 通过标准与证据 |
| --- | --- | --- | --- | --- |
| BD-01 | P0 | SIM, INT | 以两个企业的同 ID 连接、账户、任务、原始消息、标准化记录、回执和查询投影交叉访问列表、详情、搜索、分页及下载入口；伪造企业参数。 | 服务端仅返回当前企业数据；跨企业读写返回 `403` 或安全空结果，不泄露对象存在性。保存请求/响应、数据库行属主和权限拒绝审计。 |
| BD-02 | P0 | SIM, INT | 分别用定时调度器和授权人工对同一账户/时间窗口同步 `BD-BASE-01`，再并发触发和网络重试。 | 两入口均创建可追溯任务，具备触发方式、幂等键、请求编号、窗口和审计；只产生一组业务事实，重试返回既有任务/结果而非绕过持久化。 |
| BD-03 | P0 | SIM, INT | 对拉取、提交、回执查询分别在适配器调用后注入标准化失败、业务校验失败和领域写入失败。 | 原始请求/响应与同步日志先于下游数据持久化，能由任务和请求编号追溯；失败不删除或覆盖原始层，业务层不出现半写入。 |
| BD-04 | P0 | INT, PROD | 读取普通业务前端/API、业务查看者、无权用户和受控审计员对原始报文/同步日志的访问；检查网络响应、前端缓存、日志和导出。 | 普通前端与业务用户无原始层端点或字段；无权返回 `401/403`。受控审计读取仅在策略许可时提供最小化、脱敏内容并留痕；无明文凭据、私钥或 Token。 |
| BD-05 | P0 | SIM, INT | 将 `BD-BASE-01` 分别通过适配器模拟器的流水、回单、对账单、支付回执和代发结果映射到统一模型。 | 每条标准化记录含企业、来源、来源记录 ID、映射版本、来源时间及原始消息引用；业务表/前端无银行专有字段。保存映射版本、输入摘要、输出摘要和审计。 |
| BD-06 | P0 | SIM, INT, RA-DATA | 使用 `BD-DUP-01` 的重复流水号、组合重复、无流水号降级场景、页间重复、定时/手动重叠和网络重试。 | 服务端和数据库共同去重；重复返回既有标准化/业务记录及命中依据，不创建第二条业务事实。降级判定与人工复核原因可追溯。**RA-DATA** 仅用于复核真实业务字段的组合稳定性。 |
| BD-07 | P0 | SIM, INT | 对映射版本升级前后同一原始消息重跑标准化；提交未知版本、已废弃版本和字段缺失版本。 | 已处理历史保留原映射版本和原输出，不被新版本覆盖；未知/不兼容映射安全失败，原始层和日志仍可追溯，且不写错误业务事实。 |
| BD-08 | P0 | SIM, INT | 对 30 天边界前、边界时和边界后的原始报文/同步日志执行保留扫描、清理/归档和争议豁免。 | 到期策略可配置；清理或归档记录操作者/任务、范围、时间和请求编号；豁免数据不被清理。普通业务数据和审计引用不因清理损坏。最终物理删除/归档方案未决时，未获批准不得在生产执行不可逆清理。 |
| BD-09 | P0 | SIM, INT | 用 `BD-RECEIPT-01` 提交转账：依次返回 `ACCEPTED`、超时、未知、最终成功和明确失败。 | 每次操作关联任务、外部幂等号、银行侧请求号、原始回执与 `payment_attempt`；`ACCEPTED`/超时/未知不生成已付款交易、不扣/覆盖余额、不标记 `EXECUTED`。只有最终成功可驱动一次一致的交易、余额与转账状态变更。 |
| BD-10 | P0 | SIM, INT, SANDBOX | 先后发送重复成功、重复失败、成功后失败、失败后成功和乱序回执。 | 状态转换只接受合法且版本匹配的回执；终态不可被旧回执覆盖。相同外部幂等号最多一笔交易、一次余额变化和一个最终业务结果。**SANDBOX** 必须按银行提供的真实回执语义复验。 |
| BD-11 | P0 | SIM, INT, SANDBOX | 注入银行网络超时、进程重启、银行已受理但本地未知、可重试明确失败和重复查询。 | 任务进入明确可恢复状态；未知结果使用同一外部幂等号查询或进入待对账/人工处置，不能盲目发起新支付。明确可重试失败遵循退避、次数和审计；所有尝试形成一条关联链。**SANDBOX** 验证实际补单/查询协议。 |
| BD-12 | P0 | SIM, INT | 在原始落库、标准化、去重、自动入账待复核写入、支付尝试更新、交易写入、余额更新和审计写入处注入异常。 | 每一阶段要么回滚本地同一业务事务，要么留下规定的可恢复任务状态；不存在“已执行无交易”“交易无余额变化”“标准化无原始消息”或“成功无审计”状态。 |
| BD-13 | P0 | INT | 检查银行数据页面、网络请求、前端状态管理、缓存、浏览器存储和错误处理；用原始报文中故意加入银行专有字段作探针。 | 前端仅调用 FINFLOW API，查询业务表/投影；探针字段不出现在响应、DOM、缓存、控制台、路由参数或导出。未启用/空态/失败态不伪造余额、流水、回单或支付事实，失败含请求编号。 |
| BD-14 | P1 | SIM, INT, RA-DATA | 对多页窗口、时区边界、夏令时适用银行、历史补拉、账户状态变更和金额精度执行拉取/标准化。 | 分页无漏项/重复项，时间窗口与银行/系统时区有明确规则，金额精确；账户映射与企业范围正确。**RA-DATA** 仅用于真实历史数据的字段、时区和分页窗口复核。 |
| BD-15 | P1 | INT, RA-DATA | 检查临时真实业务数据在数据库、日志、审计、导出、对象存储、测试报告和支持工单中的访问、脱敏、加密、最小保留与清理。 | 仅获批角色和环境可访问；导出受限并审计；测试证据使用掩码；到期数据按批准方案清理/归档。该用例受风险接受影响，未完成专项决策不得提升为生产证据。 |
| BD-16 | P1 | INT, PROD | 触发任务失败、原始层写入失败、解析失败、去重冲突、回执乱序和保留清理；检查指标、告警、值班与审计。 | 监控按企业/任务/适配器版本聚合，不泄露敏感数据；告警携带请求编号、任务号和安全摘要。定时任务责任人、升级路径和 SLA 未决时，记录为发布风险并获得书面批准。 |
| BD-17 | P0 | SANDBOX | 使用银行提供的 SDK/API、测试证书与授权沙箱逐项验证鉴权、签名/验签、时间戳、防重放、分页、错误码、限额、超时、重复/乱序回执、查询/补单和网络出口。 | 调用仅经后端适配器；银行侧请求号、内部请求编号、外部幂等号和业务对象可双向追溯。验证证书与凭据不泄露。未完成前，真实接入状态必须为未验证，不得宣称银行直联可用。 |
| BD-18 | P0 | PROD | 候选生产环境检查适配器版本、生产端点闸门、密钥管理引用、网络白名单、日志级别、30 天策略、原始层访问策略、告警和只读烟囱。 | 不含测试凭据、真实凭据明文或前端 SDK 依赖；生产外部调用仅在另行批准的真实接入放行后允许。本 v0.2 底座验收默认不发起真实资金或银行查询。 |

## 6. 联调顺序与出口条件

1. **数据模型与隔离**：执行 BD-01、BD-04、BD-05，确认企业过滤、原始层禁入前端和映射版本追溯。
2. **同步与原始层**：执行 BD-02、BD-03、BD-08，确认手动/定时统一任务模型、先落原始层和 30 天策略。
3. **标准化与去重**：执行 BD-06、BD-07、BD-14，确认组合唯一、版本兼容和分页窗口。
4. **回执与领域一致性**：执行 BD-09 至 BD-12，确认异步状态、重复/乱序、重试与余额/交易/转账一致性。
5. **前端与可观测性**：执行 BD-13、BD-16，确认前端事实边界、请求编号、告警和失败可恢复性。
6. **风险接受与银行验证**：在专项数据使用批准后执行 BD-15；SDK/API、沙箱材料就绪后执行 BD-17；候选生产上线前执行 BD-18。

每一阶段的 P0 失败均阻断后续阶段。所有 P0 通过前，禁止将模拟器结果标记为真实银行集成可用，禁止启用真实资金相关能力。

## 7. 风险接受与银行沙箱核对表

| 事项 | 临时风险接受影响 | 必须完成的控制 | 银行 SDK/API 与沙箱到位后追加验证 |
| --- | --- | --- | --- |
| 真实业务数据副本 | 允许在获批的测试/非生产环境用于 BD-06、BD-14、BD-15，目的仅为字段映射、历史窗口、分页和脱敏复核。 | 数据最小化、加密、企业隔离、最小权限、禁止不受控导出、访问/清理审计、明确退出日期。 | 与银行返回的真实字段、分页游标、时区、编码和错误码逐项比对。 |
| 原始报文与同步日志 | 风险接受不允许将其扩散到普通日志、导出或客户端。 | 加密或受控存储、普通前端禁止访问、30 天保留与豁免、清理审计。 | 验证真实 SDK 请求/响应、签名材料和错误内容被正确隔离与脱敏。 |
| 真实凭据/私钥/Token | **不受风险接受影响，始终禁止。** | 仅由受控密钥管理服务向后端适配器注入；不得写入代码、数据库原始层、日志、审计、导出、浏览器或测试工单。 | 使用沙箱凭据验证轮换、过期、撤销、权限不足和泄露检测，不暴露值。 |
| 异步支付回执 | 模拟器可验证状态机与幂等，不能证明银行语义。 | `ACCEPTED`/未知不入终态；保留外部幂等号、回执关联与待对账处置。 | 验证真实异步回执、重复/乱序、超时查询、补单和最终人工确认流程。 |
| 定时任务与告警 | 可在非生产验证功能；责任人/SLA 尚待业务决策。 | 任务幂等、重试限制、审计、指标、请求编号和失败不重复写入。 | 用沙箱限流、维护窗口和真实错误码验证退避、告警升级和人工处置。 |

## 8. 证据与发布记录

- 候选版本、制品哈希、数据库迁移版本、适配器/映射版本：
- SIM/INT/SANDBOX/PROD 环境、企业与账户测试数据集：
- 风险接受编号、批准人、适用数据范围、到期日与清理证据：
- BD-01 至 BD-18 通过/失败、请求编号和缺陷编号：
- 原始先落库、30 天保留/豁免/清理审计证据：
- 手动/定时同步、组合去重、重复/乱序回执与重试证据：
- 业务表、支付尝试、转账、交易与余额一致性证据：
- 前端 FINFLOW API-only、原始层禁入与敏感数据抽检证据：
- 银行 SDK/API 与沙箱验证状态：未就绪 / 通过 / 不通过：
- QA 结论：通过 / 不通过：
- 安全结论：通过 / 不通过：
- 财务、运维、产品、发布批准人及时间：

## 9. 协议、编码与银行状态专项矩阵

本矩阵补充适配器接入准备，不表示已接入真实银行。除明确标注 **SANDBOX** 的项目外，均以受控模拟器、合成报文和虚拟凭据占位符验证。任何真实凭据、私钥、访问令牌、生产证书、生产网关地址或真实资金操作均不在测试数据或执行范围内。

### 9.1 Adapter 边界与原始层

| ID | 优先级 | 环境 | 检查点与操作 | 通过标准 |
| --- | --- | --- | --- | --- |
| BA-01 | P0 | SIM, INT | 以适配器契约替换模拟器实现，注入银行专有字段、协议错误和分页游标；检查业务域、通用 DTO、数据库投影和前端响应。 | 银行 SDK/API 仅能由后端 Adapter 调用；银行专有字段只留在受控原始层和 Adapter 内部，不扩散到自动入账、支付/调拨、通用业务表或前端 API。 |
| BA-02 | P0 | SIM, INT | 对拉取、提交、回执查询分别注入“适配器返回后解析失败”“标准化失败”“业务写入失败”；按任务号和 `request_id` 查询原始层、同步日志和领域数据。 | 原始响应和同步日志先落库，再允许标准化或业务写入；任何下游失败均保留受控原始追溯和安全摘要，不出现没有原始消息引用的标准化/业务事实。 |
| BA-03 | P0 | INT, PROD | 以业务查看者、无权用户、普通前端及受控审计员尝试读取原始响应正文、存储位置、同步日志全文和银行专有字段。 | 普通前端和业务 API 没有原始正文端点或字段；无权请求返回 `401/403` 或安全空结果。受控审计读取仅返回策略允许的最小化、脱敏内容并写入审计，不返回存储 URL、完整响应或凭据。 |
| BA-04 | P1 | SIM, INT | 对同一原始消息多次标准化、映射版本升级、回滚和未知映射版本执行重放。 | 标准化记录保存 `mapping_version`、原始消息引用和来源时间；历史输出不被覆盖，未知版本失败可追溯，重放遵循幂等和审计。 |

### 9.2 GBK、Base64 与敏感材料

| ID | 优先级 | 环境 | 检查点与操作 | 通过标准 |
| --- | --- | --- | --- | --- |
| BA-05 | P0 | SIM, INT, RA-DATA, SANDBOX | 使用含中文户名、摘要和特殊符号的 UTF-8 与 GBK 样本，覆盖有效字节序列、错误编码、混合编码、不可解码字节及边界截断；比较原始字节摘要、解码文本和标准化字段。 | Adapter 显式按银行文档指定字符集解码，不依赖平台默认编码；正确 GBK 中文不乱码，非法编码安全失败并保留原始层、错误分类和请求编号，不写入错误业务事实。**RA-DATA** 仅用于获批业务数据的字符集兼容性复核；**SANDBOX** 必须以银行实际报文复验。 |
| BA-06 | P0 | SIM, INT, SANDBOX | 使用有效、缺失、损坏、嵌套和超长 Base64 样本作为报文/字段；验证解码、大小限制、异常处理、原始保存和日志。 | Base64 仅在协议边界解码，失败不触发业务写入；原始内容按受控存储保存，日志仅有内容摘要和脱敏错误。不得把解码后的敏感材料回显到 API、前端、审计或导出。 |
| BA-07 | P0 | SIM, INT, SANDBOX, PROD | 在请求、响应、错误码、MAC 验证失败、证书加载失败、Token 过期和重试场景中植入唯一虚拟秘密探针；扫描应用日志、同步日志、原始层元数据、审计、错误响应、指标、导出、浏览器存储和测试证据。 | MAC、证书、私钥、Token、Authorization 值和完整账号均不会泄露；日志/审计只保留安全摘要、密钥引用版本和错误类别。真实凭据永不用于该测试；**SANDBOX** 仅使用受控沙箱凭据并验证其不暴露。 |
| BA-08 | P1 | SANDBOX, PROD | 验证证书/Token 轮换、过期、撤销、权限不足和 MAC/签名校验失败的 Adapter 行为。 | 凭据只由密钥管理/部署注入，错误快速失败且不输出值；旧凭据不可继续使用，重试不扩大权限或绕过签名校验。银行文档和沙箱材料未到位时，本项必须标为未执行。 |

### 9.3 分页、92 天窗口与任务调度

| ID | 优先级 | 环境 | 检查点与操作 | 通过标准 |
| --- | --- | --- | --- | --- |
| BA-09 | P0 | SIM, INT, RA-DATA, SANDBOX | 构造 92 个自然日窗口，覆盖首尾边界、空日、多页、页间重复、页间乱序、相同时间戳和补拉窗口；分别用手动及定时任务运行。 | 窗口起止采用明确且一致的时区/包含边界规则；分页无漏项或重复业务事实，窗口不越权跨企业/账户。**RA-DATA** 仅用于核对真实历史窗口规模与字段稳定性；**SANDBOX** 按银行最大查询窗口和分页协议复验。 |
| BA-10 | P0 | SIM, INT | 同一账户和 92 天窗口由手动与定时任务重叠触发，再对同一 `request_id` 重试、不同 `request_id` 并发触发。 | 两种触发均进入同一任务、审计和幂等模型；同请求返回已有任务，不同请求仍由组合去重兜底；不会重复写入标准化记录、自动入账事实、支付尝试或查询投影。 |
| BA-11 | P1 | SIM, INT, SANDBOX | 注入分页游标失效、重复页、空页后仍有下一页、页大小变化、银行限流和窗口超限。 | Adapter 记录分页游标/窗口摘要和失败分类，任务可恢复或安全失败；不得把不完整拉取显示为完整成功。**SANDBOX** 验证实际限流、页大小和窗口限制。 |

### 9.4 银行状态、超时与回执矩阵

状态码含义必须以银行文档为准。在文档和沙箱就绪前，下表只验证 FINFLOW 的安全中间状态、幂等和审计，不得假设任一银行码等价于付款成功。

| 银行状态样本 | 模拟器预期分类 | FINFLOW 允许状态/动作 | 必须验证 |
| --- | --- | --- | --- |
| `AAAAAAA` | 文档待确认的成功候选 | 仅在银行文档明确为最终成功且回执完整时，才允许进入最终成功路径；否则保持等待异步回执/待对账。 | 重复、乱序和重放不创建第二笔交易或余额变化；**SANDBOX** 必验真实语义。 |
| `AAAAAAE` | 文档待确认的受理/处理中候选 | 进入处理中、等待异步回执或待对账；禁止直接 `EXECUTED`。 | 轮询/查询使用同一外部幂等号；超时不重发新支付。 |
| `EEEEEEE` | 文档待确认的明确失败候选 | 记录安全失败原因，可按策略重试或人工处置；不得写入成功交易或覆盖余额。 | 重试次数、退避、审计和最终人工处置可追溯。 |
| `UNKNOWN` | 未知/无法分类 | 保持未知、处理中或待对账，阻止自动成功或自动新支付。 | 必须产生告警和请求编号；仅可使用同一外部幂等号查询/补单。 |

| ID | 优先级 | 环境 | 检查点与操作 | 通过标准 |
| --- | --- | --- | --- |
| BA-12 | P0 | SIM, INT | 对 `AAAAAAA`、`AAAAAAE`、`EEEEEEE`、`UNKNOWN` 分别注入首次回执、重复回执、乱序回执和重放回执。 | 每个回执关联任务、原始响应、同步日志、外部幂等号、银行侧请求号与审计；仅经已确认的最终成功映射才能进入 `EXECUTED`。无文档映射时四类状态都不得自动成为最终成功。 |
| BA-13 | P0 | SIM, INT, SANDBOX | 注入连接超时、读取超时、未知发送结果、进程重启、可重试明确失败和不可重试失败；检查重试和后续回执查询。 | 任务使用可恢复状态；未知发送结果不新建外部幂等号或重复支付，只能查询/补单/待对账。重试不会重复产生业务事实。**SANDBOX** 按真实超时、重试和查询协议复验。 |
| BA-14 | P0 | SIM, INT | 将最终成功、失败、未知和乱序回执分别作用于 `payment_attempt`、转账申请、交易记录和余额。 | 业务表只由领域服务一致更新：最终成功最多一笔交易和一次余额变化；失败/未知不改变成功事实；旧回执不能覆盖终态，所有变化均有审计。 |

### 9.5 追加执行与未执行项记录

| 项目 | 执行状态 | 解除条件 |
| --- | --- | --- |
| BA-01 至 BA-04 Adapter 边界、原始层与映射 | 未执行 | 候选版本、模拟器、数据库迁移和受控审计查询就绪。 |
| BA-05 至 BA-07 GBK/Base64/脱敏 | 未执行 | 合成 GBK/Base64 样本、虚拟秘密探针和日志/导出扫描路径就绪；不得使用真实凭据。 |
| BA-08 证书/Token/MAC 实际轮换 | 未执行 | 银行 SDK/API、沙箱证书/Token、签名规则和密钥管理注入流程已获授权。 |
| BA-09 至 BA-11 92 天窗口与真实分页限制 | 未执行 | 模拟器可提供 92 天多页样本；真实窗口/分页限制须等银行文档与沙箱到位。 |
| BA-12 至 BA-14 四类状态、超时和回执 | 未执行 | 模拟器状态注入和支付领域测试夹具就绪；真实状态码语义、补单和乱序规则须在银行沙箱复验。 |

执行本专项后，需将每项状态由“未执行”更新为通过、不通过或风险接受，并在第 8 节记录请求编号、适配器版本、映射版本、测试数据集、银行文档版本和缺陷编号。

## 10. 第二阶段验收矩阵（PH2）

本矩阵是第二阶段发布前的执行清单，不是已完成结果。`状态`列只允许使用“待执行”或“待银行物料”：前者表示可在 MOCK/INT/受控非生产环境执行但尚未取得证据，后者表示必须等待银行 SDK/API、沙箱、测试证书、字段/错误码文档或真实回执协议，当前不能以 MOCK 结果替代。执行后须在证据库补充实际状态和缺陷编号，不得把本表的通过标准当作通过结论。

| ID | 优先级 | 状态 | 环境/前置条件 | 操作步骤 | 可验证通过标准 | 证据要求 | 依赖/阻断条件 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PH2-BD-01 | P0 | 待执行 | SIM、INT；模拟器可注入响应、解析失败和数据库故障 | 触发六类数据同步，在原始写入完成前后分别注入解析、标准化、业务写入和审计失败；按任务号和 `request_id` 查询各层 | 原始请求/响应及同步日志独立持久化；下游失败不删除、不覆盖原始事实；原始层写入失败时不产生标准化或业务记录；每层仅通过引用关联 | 原始层主键/摘要、任务状态、数据库时间线、故障注入日志、`request_id` 和审计事件 | 原始存储隔离事务/可靠写入能力、故障注入夹具；未取得证据前不得宣称“先落库”通过 |
| PH2-BD-02 | P0 | 待执行 | SIM、INT；可访问 Adapter、原始层、统一模型、业务表/API 和前端网络记录 | 用含银行专有字段的响应走完整同步链路，分别检查 Adapter 边界、每层数据引用和前端请求 | 只能沿“原始层 -> 标准化 -> 业务表/投影 -> FINFLOW API -> 前端”单向流动；前端不能调用 Adapter、原始接口或同步日志全文；下游不能回写原始事实 | 请求拓扑、接口清单、数据库引用、前端网络/DOM 抽样和字段差异报告 | API 契约、前端构建包和可观测环境；任何反向依赖或银行字段扩散为 P0 阻断 |
| PH2-BD-03 | P0 | 待执行 | SIM、INT；支持手动、定时、并发和网络重试 | 对同一企业/连接/账户/类型/窗口同时触发手动与定时同步，重复提交同一幂等键，再使用不同请求号重试 | 手动和定时共用任务、锁、幂等和审计模型；相同幂等语义只执行一次；不同请求号由服务端组合去重兜底；不重复产生标准化、入账或查询事实 | 任务表、幂等键、锁竞争、触发方式、业务记录计数、请求/审计关联和重试记录 | 调度器、多实例/并发夹具、数据库唯一约束；未覆盖并发时发布阻断 |
| PH2-BD-04 | P0 | 待执行 | SIM、INT；可推进时间或使用隔离时钟，清理任务可审计 | 构造接收时间在 30 天内、恰到边界、已过期、豁免和清理失败的原始记录；执行预览、清理/归档和重试 | 保留依据接收时间明确；到期处理实际执行并可重试；豁免不被处理；清理/归档不删除业务记录或审计引用；每次范围、数量、操作者/任务、时间、结果和 `request_id` 都有不可篡改审计 | 时间窗口样本、清理前后计数、豁免记录、任务结果、审计事件和失败重试证据 | 清理/归档策略、权限、时钟控制和审计存储；只有 `retention_until` 没有实际处理与审计不得放行 |
| PH2-BD-05 | P0 | 待执行 | SIM、INT；模拟器支持日期窗口、分页、空页、重叠页和乱序/晚到记录 | 覆盖闭区间/开区间边界、空页后仍有下一页、页间重叠、乱序、窗口补拉及晚到数据；重复执行相邻窗口 | 起止边界、时区、游标和晚到策略有明确实现；无漏项/重复业务事实；空页不会错误结束仍有下一页的查询；乱序不改变最终事实；重叠补拉可幂等 | 输入页快照、游标/窗口摘要、标准化记录计数、边界记录、晚到处置和任务日志 | 分页注入能力及经确认的窗口规则；真实银行窗口/游标语义仍需银行物料复验 |
| PH2-BD-06 | P0 | 待执行 | SIM、INT；准备双企业、双账户和重复/缺失流水号数据 | 注入相同流水号、相同企业+账户+交易时间+金额组合、跨企业同值、页间重复和并发重复 | 去重键由服务端按企业范围计算并由数据库兜底；同企业重复只保留一条业务事实；跨企业同值互不冲突；无稳定流水号的降级判定和人工复核原因可追溯 | 去重命中依据、唯一约束/查询、企业归属、业务计数、审计及人工复核记录 | 组合键规则、精度/时区规范、企业隔离和并发测试数据；规则未定或无法证明跨企业隔离为 P0 |
| PH2-BD-07 | P0 | 待银行物料 | SIM 可先验证安全中间状态；最终状态映射需银行状态码文档和沙箱 | 分别注入 `AAAAAAA`、`AAAAAAE`、`EEEEEEE`、`UNKNOWN`，覆盖首次、重复、乱序、超时和重放；按银行文档核对原始值与内部状态 | 未取得银行定义前四类状态均不得自动视为最终成功；`UNKNOWN` 不推进成功、不新建支付；确认文档后映射有版本、原始值和审计，终态只由明确最终成功驱动 | 原始状态、映射版本、状态转换、告警、幂等号、回执和审计链；银行文档版本/沙箱结果 | 银行状态码含义、最终回执、查询/补单协议；MOCK 通过不能解除本项“待银行物料” |
| PH2-BD-08 | P0 | 待执行 | SIM、INT；所有同步、标准化、业务、API、审计路径可携带请求上下文 | 从手动/定时触发开始，沿 Adapter、原始层、标准化、业务表、失败重试、前端错误和审计逐段追踪同一 `request_id`；并行企业使用不同编号 | 每段都有同一可查询关联或受控映射；失败、重试、拒绝和清理均可追溯；`request_id` 不跨企业复用、不可枚举推断数据、不泄露银行凭据 | 全链路日志/审计索引、数据库关联、API 响应、错误页面和跨企业拒绝证据 | 统一请求上下文契约、日志/审计查询和脱敏策略；任一断点或跨企业暴露为 P1/P0 依影响评估 |
| PH2-BD-09 | P0 | 待执行 | INT；两个企业拥有相同账户标识、流水号、窗口和银行侧请求号；双企业 Token | 企业 A 读取/触发/重试自己的数据，篡改企业 B 的路径参数、Header、请求体、游标和 `request_id`；反向重复并检查副作用 | 服务端始终从会话确定企业；跨企业统一 `403` 或安全空结果，不泄露存在性；不得创建任务、写入原始/业务数据、改变状态或产生跨企业审计关联 | 双企业 API 请求/响应、数据库前后快照、审计拒绝事件、任务计数和副作用核对 | 双企业真实运行环境、认证上下文和数据库证据；静态代码检查不能替代本项 API/E2E |
| PH2-BD-10 | P0 | 待执行 | INT；准备最小权限角色、深链和原始层探针 | 用无权、查询者、同步操作员、审计员和管理员访问列表、详情、深链、触发、重试、原始层、任务和审计；伪造企业/账户/资源 ID | RBAC 按企业+账户+动作服务端执行；菜单隐藏不等于授权；无权为 `401/403` 或安全空结果；普通前端/API永不返回原始正文、同步日志敏感字段或密钥 | 每角色 API/E2E 结果、深链响应、权限矩阵、审计记录和无副作用数据库快照 | 正式权限矩阵、双企业数据、浏览器/API 测试环境；前端静态隐藏不能作为放行证据 |
| PH2-BD-11 | P0 | 待执行 | INT、PROD 候选环境；有虚拟秘密探针和全渠道采集权限 | 将虚拟 Token、私钥标记、证书片段、完整账号和银行专有字段置于模拟请求/响应及异常中，扫描日志、审计、错误、指标、导出、对象存储、前端 DOM/缓存/控制台、截图和测试证据 | 所有渠道均无秘密、完整账号和不必要原始报文；仅保留脱敏摘要/引用；导出和告警不绕过脱敏；扫描结果可复现且误报已核查 | 扫描规则/版本、原始探针清单、渠道清单、命中/排除结果、导出样本、浏览器证据和审计 | 日志、审计、导出、告警、浏览器和对象存储访问均须可采集；未完成全渠道扫描不得放行 |
| PH2-BD-12 | P0 | 待执行 | SIM、INT、PROD 候选环境；MOCK 适配器调用计数、出口/配置审计可用 | 执行所有可执行 MOCK 验收并核查连接类型、特性开关、网络白名单、DNS/出口、凭据引用、任务和监控；检查日志与制品是否含真实银行物料 | 真实银行调用次数为 0；只允许 MOCK/SIM 数据源或保持未启用/快速失败；无真实凭据、生产端点、证书或 SDK 出站；监控明确标识 MOCK，不把模拟结果写成真实接入 | Adapter 调用计数/网络审计、配置快照、制品扫描、凭据引用、任务状态和发布签字 | 生产候选环境只读核查、网络/密钥管理和发布控制；真实银行 SDK/API、证书、沙箱和授权到位前不得改为真实调用 |

### 10.1 MOCK 与银行物料边界

- **MOCK/SIM/INT 可证明**：内部 Adapter 契约、原始层先落库和故障恢复、任务状态机、手动/定时幂等、分页/空页/重叠/乱序控制、组合去重、映射版本、企业隔离、RBAC、请求追踪、脱敏和前端 API-only 边界。模拟器的每个结果必须带 `MOCK` 来源标识。
- **必须等待银行物料与沙箱**：真实 SDK/API 调用方式、证书/Token/MAC/签名、防重放、真实字段和编码、银行窗口/游标/分页限制、`AAAAAAA`/`AAAAAAE`/`EEEEEEE` 的确切含义、`UNKNOWN` 查询/补单协议、限流和异步回执语义。物料未到位时，相关矩阵状态保持“待银行物料”。
- **真实银行调用仍为 0**：本阶段不得连接真实银行网络、使用真实凭据/私钥/生产证书、发送真实请求或资金指令。银行沙箱准入也必须由单独授权记录，不能从本矩阵推导已启用生产接入。
- **RA-DATA 不扩大权限**：测试/非生产真实业务数据临时风险接受仅影响获批字段映射、窗口和脱敏复核；不允许放宽真实凭据、生产端点、企业隔离、原始层访问、日志/审计/导出脱敏或 30 天清理审计要求。

### 10.2 第二阶段发布门禁

1. `PH2-BD-01` 至 `PH2-BD-06`、`PH2-BD-08` 至 `PH2-BD-12` 的 P0 证据齐全且没有未评估的 P0 缺陷；`PH2-BD-07` 在银行物料未到位时必须明确保持“待银行物料”，不得标记为真实接入通过。
2. 企业隔离、RBAC、原始层先落库、幂等去重、敏感数据全渠道扫描和真实银行调用 0 是硬门禁，静态检查、单元测试或 MOCK 页面截图不能替代 API/E2E、数据库和运行时证据。
3. 所有未执行项、银行物料依赖、RA-DATA 批准范围、清理/归档决策和残余 P1 风险须有责任人、截止日期、补验用例和发布批准；不得以“设计已覆盖”写成已通过。
4. 发布制品必须保持真实模式未启用或快速失败，前端仅使用 FINFLOW API；未完成银行 SDK/API 与沙箱验证时，发布结论只能是“内部 MOCK 底座可验收/真实银行接入待物料”，不能是“银行直联可用”。

### 10.3 本轮复验记录（2026-08-27）

本节记录的是本轮取得的证据，不改变上表的验收状态；上表仍须在正式证据库完成逐项签字后才能更新为通过或不通过。

| 范围 | 当前证据 | 结论边界 |
| --- | --- | --- |
| Adapter、MOCK 来源与分页终止 | `mvn -Dmaven.repo.local=.m2-local test`；`CiticBankDataCodecTest` 5 项、`MockBankDataAdapterTest` 2 项通过。MOCK 适配器返回稳定数据、`MOCK` 标识和终止空页。 | 仅支持 Adapter 单测/模拟行为；不证明真实银行协议、字段、签名、限流或沙箱语义。PH2-BD-02、05、12 仍待执行。 |
| 企业隔离、RBAC、前端只读边界 | `V02BackendIntegrationTest` 的双企业列表/详情/同步任务越权检查通过；服务端测试覆盖当前登录企业过滤和无权限响应。 | 这是 H2/API 证据，不替代浏览器深链、DOM/缓存/控制台和生产运行时扫描。PH2-BD-09、10、11 仍待执行。 |
| 原始层先落库与失败恢复 | `rawResponseAndFailureLogSurviveNormalizationRollback` 通过；标准化故障后原始消息和失败日志仍存在。 | 已有 H2 故障注入证据；尚未完成隔离 MySQL 事务/锁/故障验证。PH2-BD-01 仍待执行。 |
| 窗口、分页、去重、重复触发 | `bankDataPhaseTwoHandlesWindowsPaginationDedupAndRetentionCleanup` 通过：任务 `rawCount=7`、`normalizedCount=4`、`duplicateCount=3`，重复请求复用任务；`scheduledBankDataSyncReusesRequestIdentityForSameWindow` 通过。 | 仅覆盖当前 H2 夹具和实现样本；92 天边界、空页/重叠/乱序/晚到的银行真实语义及并发生产验证仍待执行。PH2-BD-03、05、06 仍待执行。 |
| 30 天清理与审计引用 | 同一集成测试验证清理开关、过期记录标记 `[PURGED]`、`purged_at`、业务流水保留和清理日志。 | 仅证明 H2 服务级行为；清理调度、失败重试、MySQL 行为和不可篡改审计仍待执行。PH2-BD-04 仍待执行。 |
| 状态映射与真实银行调用 | 当前实现/测试仅能观察 `UNKNOWN` 安全中间态和 MOCK 无外部调用语义。未调用任何真实银行。 | `AAAAAAA`、`AAAAAAE`、`EEEEEEE` 的确切银行含义、异步回执、查询/补单和 `UNKNOWN` 处理必须等银行 SDK/API、文档和沙箱；PH2-BD-07 保持“待银行物料”，PH2-BD-12 的真实调用计数运行时核查仍待执行。 |

本轮命令结果：后端 H2 测试 18/18 通过，Flyway v1-v9 在 H2 成功应用；前端 TypeScript 检查与生产构建在受控构建环境通过，构建仍有单一入口包体超过 500 kB 的性能告警。未执行 MySQL 迁移/兼容性验证、浏览器运行时敏感扫描、全渠道日志/审计/导出扫描和真实银行沙箱，前端构建通过也不能替代浏览器 E2E。当前发布结论只能是“已有 H2/API、前端构建与单测证据，PH2 银行底座验收未完成，真实银行调用为 0”。

### 10.4 收口复验（2026-08-28，依据当前 V9/前端/DevOps 口径）

下表是对当前候选实现的证据归档，不是将 PH2 矩阵状态改为通过。`已通过（范围限定）`仅表示对应命令或测试夹具的结果真实可复核；所有生产、MySQL、浏览器运行时和真实银行结论仍按“未执行”处理。

| 范围 | 状态 | 可复核证据 | 未覆盖/风险 |
| --- | --- | --- | --- |
| V9 迁移、统一投影接口与 H2 启动 | 已通过（范围限定） | `mvn -Dmaven.repo.local=.m2-local test` 输出 Flyway v1-v9 成功应用；后端测试 18/18 通过，覆盖六类统一投影路由、资源级查询权限、企业范围请求号、无权 403 和原始失败载荷不进入投影响应。 | MySQL 空库、V1-V5 增量、厂商 V7、V8/V9、重复启动和备份恢复未实测，属于 P0 发布阻断。 |
| 真实银行调用 0 / MOCK 边界 | 已通过（范围限定） | MOCK Adapter 单测及 H2 集成日志显示 `MOCK` 和无外部网络调用；当前代码未配置真实 SDK/API。 | 未做生产候选环境出站、DNS/白名单、制品和运行时计数核验。任何非零真实调用、真实凭据或生产端点出现均为 P0。当前不能据此宣称真实银行已接入。 |
| 原始层先落库 | 已通过（范围限定） | `rawResponseAndFailureLogSurviveNormalizationRollback` 通过；标准化故障后原始消息和失败日志仍可查询。 | 仅为 H2 故障注入；隔离 MySQL 事务语义、并发锁和恢复演练未执行，属于 P0。 |
| 任务幂等与手动/定时 | 已通过（范围限定） | `bankDataPhaseTwoHandlesWindowsPaginationDedupAndRetentionCleanup` 验证重复请求复用任务；`scheduledBankDataSyncReusesRequestIdentityForSameWindow` 验证定时同窗口请求身份复用。 | 多实例并发、数据库唯一约束在 MySQL、失败重试和调度告警未完成，属于 P0/P1，不能只凭单实例 H2 放行。 |
| 企业隔离与 RBAC | 已通过（API/H2 范围限定） | `statementsAndBankDataProjectionsAreIsolatedByAuthenticatedCompany` 验证双企业列表、详情、同步任务越权和无副作用；API 权限测试通过。 | 浏览器深链、每个角色的六类查询、生产 Token/会话和数据库运行时快照未全量执行；边界失败或存在性泄露为 P0，浏览器证据缺失为 P1。 |
| 六类查询 | 待执行 | 当前服务端投影资源为 `balances`、`statements`、`receipts`、`reconciliations`、`payments`、`payroll`，前端通过 FINFLOW API 查询。 | 尚无六类资源的完整 API/E2E 权限矩阵、空态/失败态、企业隔离、响应脱敏和浏览器网络证据；不得把路由存在或菜单隐藏写成通过，缺陷按 P0（越权/泄露）或 P1（功能证据缺失）登记。 |
| 浏览器烟测与响应式 | 已通过（本机 H2/MOCK 范围限定） | 本机候选服务以管理员登录；六类查询均点击“查询”，持续显示未启用/模拟边界且 DOM 未见原始报文或凭据类文本。`1440px`、`1024px`、`375px` 页面无横向溢出；375px 导航抽屉可展开至数据查询并在选择页面后关闭，主要查询/同步按钮为 `44px`，控制台无错误。 | 未覆盖无权已登录角色的 403 深链、每个角色/企业矩阵、服务端失败/重试、浏览器缓存、网络载荷、下载/导出和生产候选环境；不得据此将六类查询整体改为通过。 |
| 30 天清理 | 已通过（H2 服务级范围限定） | 集成测试验证过期记录 `[PURGED]`、`purged_at`、业务流水保留和清理日志。 | MySQL、调度运行、失败重试、豁免、不可篡改审计和告警未执行；不能仅凭 `retention_until` 放行，清理误删或无审计为 P0。 |
| 敏感信息扫描 | 部分执行（本机浏览器范围限定） | 六类查询 DOM 与控制台检查未见原始报文、同步失败载荷或凭据类文本。 | 浏览器缓存/网络载荷，以及日志、审计、导出、指标、告警、对象存储和制品扫描未完成，属于 P1；秘密或完整原始报文实际泄露升级为 P0。 |

本轮发布结论：不放行 v0.2 生产发布。必须先完成隔离 MySQL/CI 迁移与重复启动、六类查询 API/E2E、浏览器及全渠道敏感扫描、清理审计/告警和 DevOps 发布门禁。真实银行调用保持 `0`，银行 SDK/API 与沙箱仍须独立授权和单独验收。

### 10.5 产品模块收口复验（2026-08-31）

V11 新增规则与映射、结账管理和统一审计中心。后端集成测试已覆盖规则写入/启用、`closing:view` 与 `closing:manage` 分离、账期企业范围和审计查询；前端生产构建通过。当前证据仍是 H2/MOCK 范围，不表示真实银行、金蝶或飞书已经接入。

| 模块 | 当前状态 | 下一步门禁 |
| --- | --- | --- |
| 规则与映射 | H2/API 已验证 | 财务确认规则责任人、金额阈值、科目和凭证模板；再覆盖规则实际命中到复核/制证的端到端案例。 |
| 结账管理 | H2/API 已验证 | 补差异责任人、结账日历、重开/更正策略和浏览器操作审阅；MySQL 迁移待隔离环境验证。 |
| 审计中心 | 基础版已验证 | 汇聚登录、权限拒绝、同步、复核、制证、飞书和清理审计；完成日志/导出/浏览器缓存敏感扫描。 |
| V11 迁移 | H2 已验证 | CI 和隔离 MySQL 完成空库、增量、重复启动、备份恢复后，才可纳入候选发布。 |
