# FINFLOW 银行连接模块去模拟化清理 — 交付单（2026-09-04）

> 交付类型：A+B+C 全档去模拟化（代码收口 + 测试隔离 + 线上复验）
> 对应诉求：**真实接测试环境后无法区分真实数据**——根源是模拟时代配置/兜底路由/入口残留，而非库内数据
> 关联提交链：`e35ef7e` → `eded89a` → `c73ff5e` → `d664aa9`（sync）→ `84b029c`（CI V15+jacoco）→ `75f6b2e`（FK 顺序）→ `26899bb`（FK 全子表）

## 一、交付物

| 产物 | 文件 | MD5 | 大小 |
|---|---|---|---|
| 后端 jar | `tmp/finflow-release-20260904-mockclean/finance-system-backend-20260904-1025.jar` | `11a6456c465c79fd132ffd440b0450f9` | 83,657,037 B |
| 前端 dist | `tmp/finflow-release-20260904-mockclean/web-dist.tar.gz` | `8625db4359968fb71e2ef18778ec8caf` | 453,158 B |
| 备份弹药 | ECS `/opt/finflow/app.jar.bak-20260904-mockclean` + `web-dist.bak-20260904-mockclean/` | — | — |

## 二、改动清单（代码侧已提交并推送，CI 全程守卫）

| 编号 | 内容 | 文件 |
|---|---|---|
| A1 | Registry 拆除 `provider_MOCK` / 裸 `MOCK` 兜底路由 → fail-closed 400 | `BankDataAdapterRegistry.java` + RegistryTest/AggregationServiceTest |
| B1 | 4 个 mock 适配器（Mock/CmbMock/BrandMock/CiticMock）从 `src/main` 移入 `src/test`，去掉 `@Component` 与 `matchIfMissing` 注册；`application.yml` 删除 `bankdata.adapter.mode` | 4 个 adapter + yml |
| B2 | statements 前端 MOCK 导入入口下线（仅 JSON），表单类型/提示文案同步 | `frontend/.../statements/pages.tsx` |
| C1 | 前端 `simulated` 字段与 `isSimulatedStatus` 分支清理 | `bank-access/types.ts`、`shared/format.ts` |
| C2 | **数据血缘透出**：投影 DTO 新增 `bankRequestNo`（银行原始请求号）+ `taskStatus`（同步任务状态），列表页与详情展示 | DTO ×2 + `BankDataQueryService` + `BankDataQueryPage.tsx` |
| A2 | 生产 2 个 UNKNOWN 空壳任务成因确认（联调早期网关未通，raw=0），新版本真实同步 SUCCEEDED 覆盖窗口语义 | 线上操作 |
| CI | V15 纳入 expected_versions；mock guard 改「main 无 test 有」；h2/mysql migration 断言 14→15；backend verify 加 `-Djacoco.skip=true`（JaCoCo 无法插桩 jsqlparser） | `.github/workflows/ci.yml` |
| 测试 | CmbRealPath 隔离测试 @BeforeEach 用 JDBC 清空 5 张 landing-zone 表（消除跨类数据污染与 FK 顺序） | `CmbRealPathTenantIsolationIntegrationTest.java` |

## 三、部署动作（ECS 101.200.72.87 /opt/finflow）

1. 上传 jar + dist.tar.gz → `cp` 覆盖 `app.jar`、tar 解压进 `web-dist/`（**未换目录**，nginx bind mount inode 不变，无需 recreate nginx）
2. `.env` 删除死配置 `BANKDATA_ADAPTER_MODE=mock`；`docker-compose.yml` 对应行注释（新版 Spring 无消费方）
3. `docker compose up -d --build app` → Flyway 自动应用 **V15（retire seeded citic accounts）**
4. 容器内 `/app/app.jar` MD5 = `11a6456c…` ✓ 与交付一致

## 四、线上复验结果（2026-09-04 10:35–10:40）

| 检查项 | 结果 |
|---|---|
| 容器状态 | finflow-app Up (healthy)，Started in 9.47s，V15 应用成功（validated 15 / applied v15） |
| 连接总览 `GET /api/operations/connections` | `status=REAL, enabled=true`，「已连接真实银行直联（CMB）」；响应无 `simulated` |
| 余额投影 | total=3，行级 `bankRequestNo=20260903171623776N8IGRW7GHQ`、`taskStatus=SUCCEEDED`、`status=VALID`；无 `simulated` |
| 流水投影 | total=4，行级同样带真实请求号 + SUCCEEDED；无 `simulated` |
| A2 窗口 sync | 同窗口触发被 **sync_key 幂等**拦截返回旧任务（4:0:CMB:窗口）；换 09-03 全天新窗口 → 任务 4 **SUCCEEDED raw=1 normalized=1**，请求号 `20260904103942992` |
| 兜底路由 | 新版 Registry fail-closed：未知/不可用适配器 400，模拟数据无法再混入 |

**结论**：页面上的 4 笔流水（0.01/0.02/0.22/0.02 元）此前已确认为招行测试环境真实数据；本次部署后每行均可追溯到银行原始请求号与 SUCCEEDED 任务，「无法区分真实数据」问题闭合。

## 五、回滚

```bash
cd /opt/finflow
cp app.jar.bak-20260904-mockclean app.jar
# web-dist 如需回滚：清空后从 web-dist.bak-20260904-mockclean/ 恢复
docker compose up -d --build app
```

## 六、遗留登记（不阻塞本次交付）

- P2 系列（CI grep 断言易碎 / operations 5.6%·kingdee 8.8% 覆盖率盲区 / 前端主 chunk 795.70kB）已登 `docs/pending-fixes.md` FIX-002
- CITIC `CITIC_MOCK_MODE`、金蝶 `KINGDEE_MOCK_MODE` 属其他模块模拟开关，未在本次范围，另行评估
- 旧的 2 个 UNKNOWN 空壳任务保留在库（0 行、无风险），如需归档可人工核销
